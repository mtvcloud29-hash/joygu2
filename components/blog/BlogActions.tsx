"use client";
import { useState } from "react";
export function BlogActions({title}:{title:string}){const [copied,setCopied]=useState(false);const url=typeof window!=="undefined"?window.location.href:"";const copy=async()=>{await navigator.clipboard.writeText(url);setCopied(true);setTimeout(()=>setCopied(false),1500)};return <div className="mt-8 flex flex-wrap gap-2"><a className="button-quiet" target="_blank" rel="noreferrer" href={`https://www.facebook.com/sharer/sharer.php?u=${encodeURIComponent(url)}`}>Facebook</a><a className="button-quiet" target="_blank" rel="noreferrer" href={`https://twitter.com/intent/tweet?text=${encodeURIComponent(title)}&url=${encodeURIComponent(url)}`}>X</a><a className="button-quiet" target="_blank" rel="noreferrer" href={`https://wa.me/?text=${encodeURIComponent(`${title} ${url}`)}`}>WhatsApp</a><button type="button" className="button-quiet" onClick={()=>void copy()}>{copied?"Copied":"Copy link"}</button></div>}
