import { ArrowUpRight, type LucideIcon } from "lucide-react";
export function MetricCard({ label, value, detail, icon: Icon, tone = "light" }: { label: string; value: string; detail: string; icon: LucideIcon; tone?: "light" | "dark" }) { return <div className={`relative overflow-hidden rounded-3xl border p-5 ${tone === "dark" ? "border-ink bg-ink text-white" : "border-clay-200 bg-card text-ink"}`}><div className="flex items-start justify-between"><span className={`flex h-9 w-9 items-center justify-center rounded-xl ${tone === "dark" ? "bg-white/10 text-clay-300" : "bg-clay-100 text-clay-400"}`}><Icon size={17} /></span><ArrowUpRight size={15} className={tone === "dark" ? "text-clay-300" : "text-muted"} /></div><p className={`mt-8 text-[10px] font-bold uppercase tracking-[.16em] ${tone === "dark" ? "text-white/60" : "text-muted"}`}>{label}</p><p className="mt-2 text-2xl font-semibold tracking-[-.04em]">{value}</p><p className={`mt-2 text-xs ${tone === "dark" ? "text-white/60" : "text-muted"}`}>{detail}</p></div>; }
