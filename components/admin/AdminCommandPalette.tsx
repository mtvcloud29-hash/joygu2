"use client";
import { Command, Search } from "lucide-react";
import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
const commands = [{ label: "Overview", hint: "Go to dashboard", href: "/admin" }, { label: "Products", hint: "Manage catalog and inventory", href: "/admin/products" }, { label: "Shopfront", hint: "Open storefront", href: "/products" }, { label: "Orders", hint: "Review recent orders", href: "/account" }];
export function AdminCommandPalette() { const [open, setOpen] = useState(false); const [query, setQuery] = useState(""); const router = useRouter(); useEffect(() => { const onKey = (event: KeyboardEvent) => { if ((event.metaKey || event.ctrlKey) && event.key.toLowerCase() === "k") { event.preventDefault(); setOpen((current) => !current); } if (event.key === "Escape") setOpen(false); }; window.addEventListener("keydown", onKey); return () => window.removeEventListener("keydown", onKey); }, []); const results = commands.filter((command) => `${command.label} ${command.hint}`.toLowerCase().includes(query.toLowerCase())); return <><button type="button" onClick={() => setOpen(true)} className="inline-flex items-center gap-3 rounded-full border border-clay-200 bg-card px-3 py-2 text-xs text-muted transition hover:border-clay-300 hover:text-ink"><Search size={14} /><span className="hidden sm:inline">Search workspace</span><kbd className="hidden rounded bg-clay-100 px-1.5 py-0.5 text-[10px] sm:inline">⌘K</kbd></button>{open && <div className="fixed inset-0 z-[90] flex items-start justify-center bg-ink/35 px-4 pt-[14vh] backdrop-blur-sm" onClick={() => setOpen(false)}><div className="w-full max-w-lg overflow-hidden rounded-3xl border border-clay-200 bg-card shadow-soft" onClick={(event) => event.stopPropagation()}><div className="flex items-center gap-3 border-b border-clay-200 px-5"><Search size={17} className="text-muted" /><input autoFocus value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search commands…" className="h-14 flex-1 bg-transparent text-sm text-ink outline-none" /><button type="button" onClick={() => setOpen(false)} className="text-xs text-muted">Esc</button></div><div className="p-2">{results.map((command) => <button type="button" key={command.href} onClick={() => { setOpen(false); router.push(command.href as never); }} className="flex w-full items-center gap-3 rounded-2xl px-3 py-3 text-left transition hover:bg-clay-100"><span className="flex h-8 w-8 items-center justify-center rounded-xl bg-clay-100 text-clay-400"><Command size={15} /></span><span><span className="block text-sm font-semibold text-ink">{command.label}</span><span className="block text-xs text-muted">{command.hint}</span></span></button>)}{!results.length && <p className="px-3 py-8 text-center text-sm text-muted">No command found.</p>}</div></div></div>}</>;
}
