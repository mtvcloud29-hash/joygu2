"use client";
import { useState } from "react";
import Link from "next/link";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Button } from "@/components/ui/button";
export default function AddressesPage() { const [message, setMessage] = useState(""); const [pending, setPending] = useState(false); async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setPending(true); const data = Object.fromEntries(new FormData(event.currentTarget)); const response = await fetch("/api/addresses", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(data) }); const body = await response.json() as { ok?: boolean; error?: string }; setPending(false); setMessage(body.ok ? "Address saved." : body.error ?? "Unable to save address."); if (body.ok) event.currentTarget.reset(); } return <main className="container-shell py-16 lg:py-24"><Link href="/account" className="text-xs font-semibold text-muted">← Back to account</Link><h1 className="section-title mt-8">Address <em className="font-normal text-clay-400">book.</em></h1><div className="surface mt-10 max-w-2xl p-6 sm:p-10"><form onSubmit={submit} className="grid gap-5 sm:grid-cols-2"><div><Label>Label</Label><Input name="label" defaultValue="Home" required /></div><div><Label>Name</Label><Input name="name" required /></div><div><Label>Phone</Label><Input name="phone" inputMode="numeric" required /></div><div className="sm:col-span-2"><Label>Address</Label><Input name="line1" required minLength={10} /></div><div><Label>City</Label><Input name="city" required /></div><div><Label>State</Label><Input name="state" required /></div><div><Label>PIN code</Label><Input name="pincode" inputMode="numeric" required /></div><div className="sm:col-span-2"><Button disabled={pending}>{pending ? "Saving…" : "Save address"}</Button>{message && <p className="mt-3 text-sm text-muted">{message}</p>}</div></form></div></main>; }
