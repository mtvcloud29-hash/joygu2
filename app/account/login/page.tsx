"use client";
import Link from "next/link";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Button } from "@/components/ui/button";
export default function LoginPage() { const router = useRouter(); const [error, setError] = useState(""); const [pending, setPending] = useState(false); async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setPending(true); setError(""); const body = Object.fromEntries(new FormData(event.currentTarget)); const response = await fetch("/api/auth/login", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(body) }); const data = await response.json() as { ok?: boolean; error?: string }; setPending(false); if (!response.ok) return setError(data.error ?? "Unable to sign in."); router.push("/account"); router.refresh(); } return <main className="container-shell flex min-h-[65vh] items-center justify-center py-16"><div className="w-full max-w-md"><p className="eyebrow">Welcome back</p><h1 className="section-title mt-4">Sign in to your<br /><em className="font-normal text-clay-400">Joyguru account.</em></h1><form onSubmit={submit} className="mt-10 space-y-5"><div><Label htmlFor="email">Email</Label><Input id="email" name="email" type="email" required /></div><div><div className="flex justify-between"><Label htmlFor="password">Password</Label><Link href="/account/forgot-password" className="text-xs text-clay-400">Forgot password?</Link></div><Input id="password" name="password" type="password" required /></div>{error && <p className="text-sm text-red-700">{error}</p>}<Button className="w-full" disabled={pending}>{pending ? "Signing in…" : "Sign in"}</Button></form><div className="my-6 flex items-center gap-3 text-xs text-muted"><span className="h-px flex-1 bg-clay-200" /><span>or continue with</span><span className="h-px flex-1 bg-clay-200" /></div><a href="/api/auth/google" className="button-quiet w-full"><span className="font-bold text-[#4285F4]">G</span>Continue with Google</a><p className="mt-7 text-center text-sm text-muted">New to Joyguru? <Link href="/account/signup" className="font-semibold text-clay-400">Create an account</Link></p></div></main>; }
