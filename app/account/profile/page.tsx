import Link from "next/link";
import { redirect } from "next/navigation";
import { getCurrentUser } from "@/lib/auth";
export const dynamic = "force-dynamic";
export default async function AccountProfilePage() { const user = await getCurrentUser(); if (!user) redirect("/account/login"); return <main className="container-shell py-12 lg:py-20"><Link href="/account" className="text-xs font-semibold text-muted">← Account</Link><p className="eyebrow mt-8">Your details</p><h1 className="section-title mt-3">Profile.</h1><section className="surface mt-10 max-w-2xl p-7"><div className="grid gap-6 sm:grid-cols-2"><div><p className="eyebrow">Name</p><p className="mt-2 text-sm font-semibold">{user.name}</p></div><div><p className="eyebrow">Email</p><p className="mt-2 text-sm font-semibold">{user.email}</p></div><div><p className="eyebrow">Phone</p><p className="mt-2 text-sm font-semibold">{user.phone ?? "Not added"}</p></div><div><p className="eyebrow">Reward points</p><p className="mt-2 text-sm font-semibold">{user.rewardPoints}</p></div></div><p className="mt-8 border-t border-clay-200 pt-5 text-xs leading-5 text-muted">Profile edits are protected by your authenticated account session.</p></section></main>; }
