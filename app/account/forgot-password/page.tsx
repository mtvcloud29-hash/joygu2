"use client";
import Link from "next/link";
import { useState } from "react";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Button } from "@/components/ui/button";
export default function ForgotPasswordPage() { const [message, setMessage] = useState(""); const [pending, setPending] = useState(false); async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setPending(true); const body = Object.fromEntries(new FormData(event.currentTarget)); const response = await fetch("/api/auth/forgot-password", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(body) }); const data = await response.json() as { message?: string; error?: string }; setPending(false); setMessage(data.message ?? data.error ?? "Please try again."); } return <main className="container-shell flex min-h-[65vh] items-center justify-center py-16"><div className="w-full max-w-md"><p className="eyebrow">Password reset</p><h1 className="section-title mt-4">A fresh start,<br /><em className="font-normal text-clay-400">if you need it.</em></h1><p className="body-copy mt-6">Enter your email and, if we find an account, we’ll send a secure reset link.</p><form onSubmit={submit} className="mt-8 space-y-5"><div><Label>Email</Label><Input name="email" type="email" required /></div><Button className="w-full" disabled={pending}>{pending ? "Sending…" : "Send reset link"}</Button>{message && <p className="text-sm text-muted">{message}</p>}</form><Link href="/account/login" className="mt-7 block text-center text-sm font-semibold text-clay-400">Back to sign in</Link></div></main>; }
