import type { Metadata } from "next";
import { Mail, MapPin, MessageCircle, Phone } from "lucide-react";
import { site } from "@/lib/constants";
import { getPublicSettings } from "@/lib/settings";
import { ContactForm } from "@/components/contact/ContactForm";
export const metadata: Metadata = { title: "Contact", description: "Get in touch with Joyguru Enterprise." };
export const dynamic = "force-dynamic";
export default async function ContactPage() { const settings = await getPublicSettings(); const email = settings.contact.email || site.email; const phone = settings.contact.supportNumber || site.phone; const whatsapp = settings.contact.whatsapp || site.whatsapp; const address = settings.contact.officeAddress || site.address.join(", "); return <main className="container-shell py-16 lg:py-24"><div className="grid gap-14 lg:grid-cols-[.7fr_1.3fr]"><div><p className="eyebrow">Say hello</p><h1 className="section-title mt-4">Let’s make<br /><em className="font-normal text-clay-400">something good.</em></h1><p className="body-copy mt-6 max-w-sm">For product questions, wholesale, custom work or simply to say hello, write to us. We read everything.</p><div className="mt-10 space-y-5 text-sm text-muted"><a href={`mailto:${email}`} className="flex items-center gap-3 hover:text-clay-400"><Mail size={17} className="text-clay-400" />{email}</a><a href={`tel:+91${phone}`} className="flex items-center gap-3 hover:text-clay-400"><Phone size={17} className="text-clay-400" />+91 {phone}</a><a href={`https://wa.me/91${whatsapp}`} target="_blank" rel="noreferrer" className="flex items-center gap-3 hover:text-clay-400"><MessageCircle size={17} className="text-clay-400" />WhatsApp us</a><div className="flex items-start gap-3"><MapPin size={17} className="mt-0.5 text-clay-400" /><span>{address.split(",").map((line) => <span key={line} className="block">{line.trim()}</span>)}</span></div><p className="border-t border-clay-200 pt-5 text-xs">{settings.contact.businessHours}</p></div></div><div className="surface p-6 sm:p-10"><ContactForm /></div></div></main>; }
