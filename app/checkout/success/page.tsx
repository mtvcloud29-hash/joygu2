import Link from "next/link";
export default async function CheckoutSuccess({ searchParams }: { searchParams: Promise<{ order?: string }> }) { const { order } = await searchParams; return <main className="container-shell flex min-h-[65vh] items-center justify-center py-16 text-center"><div className="max-w-lg"><span className="mx-auto flex h-14 w-14 items-center justify-center rounded-full bg-clay-300 text-white">✓</span><p className="eyebrow mt-8">Order confirmed</p><h1 className="section-title mt-4">Thank you for<br /><em className="font-normal text-clay-400">choosing handmade.</em></h1><p className="body-copy mt-6">Your order {order && <strong className="text-ink">{order}</strong>} is with our studio now. We’ll email you as it moves from clay to your doorstep.</p><div className="mt-8 flex flex-wrap justify-center gap-3"><Link href="/products" className="button-primary">Keep exploring</Link><Link href="/track-order" className="button-quiet">Track order</Link></div></div></main>; }
