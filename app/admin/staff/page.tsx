"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
import { Plus } from "lucide-react";
import { Input } from "@/components/ui/input";
import { Button } from "@/components/ui/button";
export default function StaffPage(){const [staff,setStaff]=useState<{id:string;name:string;email:string;role:string;isActive:boolean;lastLoginAt:string|null}[]>([]);const [form,setForm]=useState({name:"",email:"",role:"STAFF"});const load=async()=>{const r=await fetch('/api/admin/staff');if(r.ok)setStaff((await r.json()).staff)};useEffect(()=>{void load()},[]);const add=async(e:React.FormEvent)=>{e.preventDefault();const r=await fetch('/api/admin/staff',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(form)});if(r.ok){setForm({name:"",email:"",role:"STAFF"});await load()}};return <main className="container-shell py-12 lg:py-20"><Link href="/admin" className="text-xs font-semibold text-muted">← Dashboard</Link><div className="mt-7 flex justify-between"><div><p className="eyebrow">Admin / Team</p><h1 className="section-title mt-3">Staff.</h1></div><Link href={"/admin/roles" as never} className="button-quiet">Roles & permissions</Link></div><div className="mt-8 grid gap-8 lg:grid-cols-[1fr_340px]"><section className="surface divide-y divide-clay-200">{staff.map(user=><Link href={`/admin/staff/${user.id}` as never} key={user.id} className="flex justify-between gap-4 p-6 hover:bg-clay-50"><div><p className="font-semibold">{user.name}</p><p className="mt-1 text-xs text-muted">{user.email}</p></div><span className="text-xs text-clay-500">{user.role} · {user.isActive?'Active':'Disabled'}</span></Link>)}</section><form onSubmit={add} className="surface space-y-4 p-6"><p className="eyebrow">Invite staff</p><Input placeholder="Name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/><Input type="email" placeholder="Email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/><select value={form.role} onChange={e=>setForm({...form,role:e.target.value})} className="h-12 w-full rounded-xl border border-clay-200 bg-white px-4 text-sm"><option>STAFF</option><option>ADMIN</option></select><Button type="submit"><Plus size={15}/>Create staff</Button></form></div></main>}
