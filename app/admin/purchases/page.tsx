"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
export default function PurchasesPage() { const [suppliers, setSuppliers] = useState<{ id: string; company: string }[]>([]); const [warehouses, setWarehouses] = useState<{ id: string; name: string }[]>([]); const [products, setProducts] = useState<{ id: string; name: string }[]>([]); const [form, setForm] = useState({ supplierId: "", warehouseId: "", productId: "", invoiceNumber: "", purchaseDate: new Date().toISOString().slice(0,10), quantity: "", purchasePrice: "", gst: "0", batchNumber: "", notes: "" }); useEffect(() => { void Promise.all([fetch("/api/admin/suppliers").then((response) => response.json()), fetch("/api/admin/warehouses").then((response) => response.json()), fetch("/api/admin/products").then((response) => response.json())]).then(([supplierBody, warehouseBody, productBody]) => { setSuppliers((supplierBody as { suppliers: typeof suppliers }).suppliers ?? []); setWarehouses((warehouseBody as { warehouses: typeof warehouses }).warehouses ?? []); setProducts(((productBody as { products: { id: string; name: string }[] }).products ?? [])); }); }, []); const save = async (event: React.FormEvent) => { event.preventDefault(); const response = await fetch("/api/admin/purchases", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ supplierId: form.supplierId, warehouseId: form.warehouseId, invoiceNumber: form.invoiceNumber, purchaseDate: form.purchaseDate, notes: form.notes, items: [{ productId: form.productId, quantity: Number(form.quantity), purchasePrice: Number(form.purchasePrice), gst: Number(form.gst), batchNumber: form.batchNumber }] }) }); if (response.ok) setForm({ ...form, invoiceNumber: "", quantity: "", purchasePrice: "", gst: "0", batchNumber: "", notes: "" }); }; return <main className="container-shell py-12 lg:py-20"><a href="/admin/inventory" className="text-xs font-semibold text-muted">← Inventory</a><h1 className="section-title mt-8">Purchase entries.</h1><form onSubmit={save} className="surface mt-10 max-w-3xl space-y-5 p-6 sm:p-10"><div className="grid gap-5 sm:grid-cols-2"><div><Label>Supplier</Label><select value={form.supplierId} onChange={(event) => setForm({ ...form, supplierId: event.target.value })} required className="h-12 w-full rounded-xl border border-clay-200 bg-white px-4 text-sm"><option value="">Select supplier</option>{suppliers.map((item) => <option key={item.id} value={item.id}>{item.company}</option>)}</select></div><div><Label>Warehouse</Label><select value={form.warehouseId} onChange={(event) => setForm({ ...form, warehouseId: event.target.value })} required className="h-12 w-full rounded-xl border border-clay-200 bg-white px-4 text-sm"><option value="">Select warehouse</option>{warehouses.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></div><div><Label>Product</Label><select value={form.productId} onChange={(event) => setForm({ ...form, productId: event.target.value })} required className="h-12 w-full rounded-xl border border-clay-200 bg-white px-4 text-sm"><option value="">Select product</option>{products.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></div><div><Label>Invoice number</Label><Input value={form.invoiceNumber} onChange={(event) => setForm({ ...form, invoiceNumber: event.target.value })} required /></div><div><Label>Purchase date</Label><Input type="date" value={form.purchaseDate} onChange={(event) => setForm({ ...form, purchaseDate: event.target.value })} required /></div><div><Label>Batch number</Label><Input value={form.batchNumber} onChange={(event) => setForm({ ...form, batchNumber: event.target.value })} required /></div><div><Label>Quantity</Label><Input type="number" value={form.quantity} onChange={(event) => setForm({ ...form, quantity: event.target.value })} required /></div><div><Label>Purchase price</Label><Input type="number" value={form.purchasePrice} onChange={(event) => setForm({ ...form, purchasePrice: event.target.value })} required /></div><div><Label>GST</Label><Input type="number" value={form.gst} onChange={(event) => setForm({ ...form, gst: event.target.value })} /></div></div><div><Label>Notes</Label><Input value={form.notes} onChange={(event) => setForm({ ...form, notes: event.target.value })} /></div><Button type="submit">Receive purchase</Button></form></main>; }
