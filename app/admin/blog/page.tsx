"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
import { Plus, Search } from "lucide-react";
import { Input } from "@/components/ui/input";
export default function AdminBlogPage() { const [posts, setPosts] = useState<{ id:string; title:string; slug:string; status:string; featured:boolean; readingTime:number; updatedAt:string; _count:{comments:number}; category:{name:string}|null }[]>([]); const [q,setQ]=useState(""); const load=async()=>{const r=await fetch(`/api/admin/blog?q=${encodeURIComponent(q)}`);if(r.ok)setPosts((await r.json()).posts)};useEffect(()=>{void load()},[q]); // eslint-disable-line react-hooks/exhaustive-deps
  return <main className="container-shell py-12 lg:py-20"><Link href="/admin" className="text-xs font-semibold text-muted">← Dashboard</Link><div className="mt-7 flex justify-between gap-4"><div><p className="eyebrow">CMS / Journal</p><h1 className="section-title mt-3">Blog.</h1></div><Link href={"/admin/blog/new" as never} className="button-primary"><Plus size={16}/>New post</Link></div><div className="relative mt-10 max-w-sm"><Search size={16} className="absolute left-3 top-3.5 text-muted"/><Input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search posts" className="pl-9"/></div><div className="surface mt-6 overflow-x-auto"><table className="w-full min-w-[700px] text-left text-sm"><thead className="border-b border-clay-200 text-xs uppercase tracking-[.14em] text-muted"><tr><th className="p-5">Title</th><th className="p-5">Category</th><th className="p-5">Status</th><th className="p-5">Reading time</th><th className="p-5">Comments</th></tr></thead><tbody className="divide-y divide-clay-200">{posts.map(post=><tr key={post.id}><td className="p-5"><Link href={`/admin/blog/${post.id}` as never} className="font-semibold text-ink hover:text-clay-400">{post.title}</Link><p className="mt-1 text-xs text-muted">/{post.slug}</p></td><td className="p-5 text-muted">{post.category?.name ?? "—"}</td><td className="p-5"><span className="rounded-full bg-clay-100 px-3 py-1 text-xs text-clay-500">{post.status}</span></td><td className="p-5 text-muted">{post.readingTime} min</td><td className="p-5 text-muted">{post._count.comments}</td></tr>)}</tbody></table>{!posts.length&&<p className="p-12 text-center text-sm text-muted">No posts yet.</p>}</div></main>}
