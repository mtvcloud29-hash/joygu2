import Link from "next/link";
import Image from "next/image";
import { prisma } from "@/lib/prisma";
export const dynamic="force-dynamic";
export default async function BlogPage(){const posts=await prisma.blogPost.findMany({where:{status:"PUBLISHED",publishedAt:{lte:new Date()}},orderBy:[{featured:"desc"},{publishedAt:"desc"}],take:20,include:{category:true,author:{select:{name:true}}}});return <main className="container-shell py-16 lg:py-24"><p className="eyebrow">The journal</p><h1 className="section-title mt-4">Notes from the<br/><em className="font-normal text-clay-400">workshop.</em></h1><div className="mt-14 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">{posts.map(post=><Link key={post.id} href={`/blog/${post.slug}` as never} className="group"><div className="relative aspect-[1.2] overflow-hidden rounded-3xl bg-clay-100">{post.featuredImage||post.coverImage?<Image src={post.featuredImage||post.coverImage||""} alt={post.title} fill sizes="33vw" className="object-cover transition duration-700 group-hover:scale-105"/>:<div className="h-full bg-clay-100"/>}</div><p className="eyebrow mt-5">{post.category?.name??"Journal"}</p><h2 className="display mt-2 text-3xl font-semibold">{post.title}</h2><p className="mt-2 text-xs text-muted">{post.readingTime} min read</p></Link>)}{!posts.length&&<p className="text-sm text-muted">The journal is being prepared.</p>}</div></main>}
